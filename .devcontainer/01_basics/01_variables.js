const accountId = 300442
let accountEmail = "adarsh@email.com"
var accountPassword = "1223"
accountCity = "hyderabad"
/* 
we can declare without using const and var.
const means we cannot update the variable value
let means we can update and change the varable value
console.log(x) which prints only x
console.table([x,y,z]) which prints x,y,z
"use strict"; this treat all JS code as newer version
code readability should be insane
learn all datatypes
to find unique we use symbols
learn about premitives 
"typeof" this tell us their datatype 
*/